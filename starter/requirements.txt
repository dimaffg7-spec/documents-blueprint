# Минимум. Ничего лишнего: чем меньше зависимостей в юридическом конвейере,
# тем меньше поводов, чтобы он перестал собираться через полгода.
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9   # разбор форм (FastAPI требует явно)
docxtpl>=0.16             # подстановка в .docx
python-docx>=1.1          # чтение готового документа + сборка шаблонов
num2words>=0.5            # суммы прописью
PyYAML>=6.0               # конфиги типов документов
qrcode[pil]>=7.4          # платёжный QR по ГОСТ Р 56042
